<h1>Products#index</h1>

<ul>
  <% @products.each do |p| %>
    <li>
      <%= link_to p.name,
        department_product_path(department_id: @department.code, id: p.code) %>
    </li>
  <% end %>
</ul>
